{
    "project": "ajinga-test",
    "driver": "chromedriver_linux64/chromedriver",
    "screenshot": "ajinga-test",
    "steps": [
        {
            "operation": "component",
            "name": "register"
        }
    ]
}
